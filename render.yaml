services:
  - type: web
    name: broking-mis-dashboard
    runtime: python
    buildCommand: "chmod +x render-build.sh && ./render-build.sh"
    startCommand: "gunicorn server:app --bind 0.0.0.0:$PORT --timeout 300 --workers 2"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
